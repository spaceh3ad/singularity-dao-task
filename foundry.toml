[profile.default]
src = "src"
out = "out"
libs = ["lib"]

remappings = [
    "@openzeppelin/contracts=lib/openzeppelin-contracts/contracts",
]

[invariant] 
fail_on_revert = true
runs = 4
depth = 1024

[fuzz]
runs = 10000
max_test_rejects = 150000